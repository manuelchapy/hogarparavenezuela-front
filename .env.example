# Desarrollo: SIEMPRE /api (proxy Vite → backend). Evita errores CORS en :5173/:5174.
# Producción: URL completa, ej. https://api.tudominio.com/api
VITE_API_BASE_URL=/api

# Target del proxy de desarrollo (Vite) — puerto del backend (API-ENDPOINTS.md)
VITE_API_PROXY_TARGET=http://localhost:4000
